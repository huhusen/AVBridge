cmake_minimum_required(VERSION 3.25)
project(plugin-rtmp)
set(CMAKE_CXX_STANDARD 17)
aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR}/src sources)
file(GLOB LIBNAMES ../../libs/debug/*.lib)
add_library(plugin-rtmp SHARED ${sources})
target_link_libraries(plugin-rtmp PRIVATE ${LIBNAMES} )